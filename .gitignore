# personal memory must never enter this repo
memory/
# personal machine settings
settings.json
settings.local.json
settings*.json
# parked worktrees — otherwise `git ls-files --others` enumerates them as duplicate trees
.claude/worktrees/
# OS/editor noise
.DS_Store
__pycache__/
*.pyc
